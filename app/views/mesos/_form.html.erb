<%= form_for(@meso) do |f| %>
  <% if @meso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meso.errors.count, "error") %> prohibited this meso from being saved:</h2>

      <ul>
      <% @meso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :macro_id %><br>
    <%= f.number_field :macro_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes %>
  </div>
  <div class="field">
    <%= f.label :volume %><br>
    <%= f.text_field :volume %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
